<% posts.forEach(post => { %>
  <h1><%= post.title %></h1>
  <p><%= post.subtitle %> </p>
  <p><%= post.content %> </p>
  <p><%= post.year %> </p>
  <p><%= post.place %> </p>
  <% if (post.images.length) { %>
    <img src=<%= post.images[0].imageUrl %> />
  <% } %>
<%  }) %>
